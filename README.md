Just a simple RAGbot in Python, as a Jupyter notebook, that uses a local LLM via ollama, and reads in a pdf as the document.

Only written to help me understand what a RAGbot is and a bit about how they work, eg what a vector database is (this one uses chromadb).

Probably only useful to you if you want a slightly pedadogical minimal RAGbot as Jupyter notebook
